# 2026-09-03 开发日志

## 非管理员个人 Key 缺失不再告警；共享 Key 缺失告警保持

### 需求

- 共享 Key 已配置时：非管理员用户未配置个人 Key **不告警**；
- 共享 Key 未配置时：非管理员用户登录后界面告警**缺少哪个 Key**
  并提示联系管理员添加。

### 修改（`web/web_prototype.html`）

- `loadMe()` 中移除对非管理员的个人 Key info 提示
  （原「账户未配置个人 DeepSeek API Key，将使用…共享 Key」），
  仅管理员未配置个人 Key 时保留「可到账户设置填写」提示；
- 共享 Key 缺失告警沿用上一轮 `checkSharedKeys(u)` 橙色横幅：
  逐个列出缺失项（共享 DeepSeek API Key / 共享 OCR.space Key）、
  非管理员提示「请联系管理员添加后使用」、管理员附「用户管理」配置链接；
- 清理死状态 `state.accountKeyConfigured`（前端 Key 拦截移除后已无引用）。

### 验证

- `node --check` 通过（提取页面内联 JS）。
- 逻辑核对：共享已配置 + 非管理员无个人 Key → 无 toast、横幅隐藏；
  共享缺失（任一/两者）→ 横幅列出具体缺失项并提示联系管理员；
  管理员无个人 Key → 提示保留。

### 其他

- 本日仓库同时合并了 `requirements_web.txt` → `requirements.txt`
  （fastapi/uvicorn/python-multipart 并入主依赖清单），
  Dockerfile、start_dev.sh、README 已同步改为只安装 requirements.txt；
  本次改动与此无冲突。

## 移除桌面版，仓库仅保留 Web 版

### 需求

删除桌面版代码，仅保留 Web 版，并保证 Web 版功能不受影响；
requirements.txt 需在 Windows / Linux 双平台可直接安装。

### 修改

- 删除：`main.py`（桌面入口）、`ui/`（tkinter 登录/主界面/账户设置，仅被
  main.py 引用）、`utils/convert_to_pdf.py`（pywin32 Word/PowerPoint COM
  转换，全仓库无任何调用方，属死代码）；
- `requirements.txt`：移除 `pywin32`（桌面专用，Linux 无发行版导致无法安装），
  其余依赖均有双平台 wheel，Windows / Linux 可直接 `pip install -r`；
- `start_dev.sh` / `Dockerfile`：删除 `sed '/^pywin32/d'` 平台分支，
  直接安装 requirements.txt；
- 文档同步：README（版本表、目录结构、快速开始）、CLAUDE.md（项目概述）、
  docs/tech_spec.md（技术栈表去掉 tkinter/pywin32，项目结构、数据流改为
  Web 版现状）、PRD/requirements/implementation_plan 顶部加历史注记。

### 验证

- `python -m compileall` 全量语法检查通过；
- 冒烟测试：导入 main_web（触发 tasks/extractors/database/ppt_generator/
  utils 全栈导入）成功；TestClient 请求 `GET /`（307→200 登录页）、
  `GET /web/login.html` 200、`GET /web/web_prototype.html` 200、
  未登录 `GET /api/settings/shared-keys-status` 401，路由总数 26；
- 全局引用检查：代码中已无 `ui`/`convert_to_pdf`/`pywin32`/`win32com`
  任何残留引用。
