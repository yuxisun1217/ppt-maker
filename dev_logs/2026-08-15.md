# 2026-08-15 开发日志

## Web 后端骨架

- 界面原型 `web_prototype.html` 已确认（位于 `.claude/worktrees/web/`，尚未提交 git）
- 新建 `main_web.py`（FastAPI）：
  - `POST /api/upload` — 文件上传（python-multipart），保存至 `data/web_uploads/`
  - `POST /api/generate` — 触发生成，返回 task_id（占位：后台任务模拟进度）
  - `GET /api/status/{task_id}` — 查询进度（内存任务表，待接入 DB）
  - `GET /api/download/{task_id}` — 下载生成的 PPT
  - CORS 已配置为开发模式全放行
- 新建 `requirements_web.txt`：fastapi / uvicorn / python-multipart，已安装
- 冒烟测试通过：上传 → 生成 → 轮询 → 下载（200 OK）
- 启动命令：`uvicorn main_web:app --reload`（默认 127.0.0.1:8000）

## 遗留问题

- 真实生成管线（DeepSeek 提取 → ppt_generator）尚未接入，`_simulate_generation` 为占位
- 任务表为内存 dict，服务重启即丢失，需换 DB
- `web_prototype.html` 需要从 worktree 合并回主分支，并把前端调用接到真实 API
- `.claude/worktrees/` 建议加入 .gitignore
