# 2026-09-04 开发日志

## 主页「账户设置」按钮对管理员也隐藏

### 需求

管理员登录主页时不再显示「账户设置」按钮（此前仅普通用户隐藏）。

### 修改（`web/web_prototype.html`）

- `loadMe()` 中管理员分支仅保留「用户管理」入口，不再显示
  `#btn-account`；
- 管理员未配个人 Key 的提示语更新：说明将使用共享 Key，
  如需配置个人 Key 可直接访问 `/web/settings.html`（页面与后端接口保留）；
- 按钮默认 `hidden` 类不变，点击监听保留（防止隐藏后仍有引用报错）。

### 验证

- 页面内联脚本 `node --check` 全部通过；
- 逻辑核对：管理员/普通用户登录均不再显示按钮；`/web/settings.html`
  直接访问仍可用，`/api/account/keys` 接口未动。

## 备份脚本与文档入库

昨日新增 `deploy/backup.sh`（每日备份：pg_dump + web_data 卷 + .env，
默认保留 14 份）与 `docs/backup_restore.md`（备份/恢复完整手册），
README 与 CLAUDE.md 文档索引已同步，详见 commit `9d46fb8`。
